package com.alis.stockservice.utility;

import com.alis.stockservice.entity.CartItemEntity;
import com.alis.stockservice.model.CartItem;

import java.util.ArrayList;
import java.util.List;
import java.util.Objects;

public class EntityToBuilder  {


   /* public static List<CartItem> convertCartItems(List<CartItemEntity> cartItemEntities) {
        var cartItems = new ArrayList<CartItem>();
        if (Objects.isNull(cartItemEntities)) {
            return cartItems;
        }
        cartItemEntities.forEach(cartItem -> {
            cartItems.add(convertCartItemBuilder(cartItem).build());
        });
        return cartItems;
    }
*/

}
